$Math.code = {
	toString : function toString(num, base){
		num = Math.floor(num);
		var ret = [],
			j;
		do{
			
		}while();
		return ret.reverse().join('');
	},
	toInt : function toInt(str){
		return ret;
	}
}
